# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Java CI with Gradle

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_call:
  # Lets CI be started by hand, e.g. when a run failed to start and can't be retried
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: iosSimulatorArm64Test
            os: macos-latest
          - target: macosArm64Test
            os: macos-latest
          # publishToMavenLocal checks that a local publish works without signing keys
          - target: jvmTest :konstant-ksp:test :konstant-ksp-tests:test :konstant-gradle-plugin:test :konstant-ksp:publishToMavenLocal :konstant-gradle-plugin:publishToMavenLocal
            os: ubuntu-latest
          - target: jsNodeTest
            os: ubuntu-latest
          - target: jsBrowserTest wasmJsNodeTest wasmJsBrowserTest
            os: ubuntu-latest
          - target: linuxX64Test
            os: ubuntu-latest
          - target: testAndroidHostTest
            os: ubuntu-latest
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v7
    - name: Validate Gradle Wrapper
      uses: gradle/actions/wrapper-validation@v6
    - uses: actions/cache@v6
      with:
        path: |
          ~/.konan
        key: ${{ runner.os }}-${{ hashFiles('**/.lock') }}
    - name: Set up JDK 21
      uses: actions/setup-java@v6
      with:
        java-version: '21'
        distribution: 'temurin'
    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v6
    - name: Build with Gradle
      run: ./gradlew ${{ matrix.target }} --continue

  # Fails when the public API no longer matches the dumps in each module's api/ folder.
  # After an intended API change, run ./gradlew updateKotlinAbi and commit the new dumps.
  api:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v7
    - name: Set up JDK 21
      uses: actions/setup-java@v6
      with:
        java-version: '21'
        distribution: 'temurin'
    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v6
    - name: Check public API
      run: ./gradlew checkKotlinAbi

  # Builds the merged API docs (./gradlew dokkaGenerate) so broken KDoc links fail the PR.
  # The generated site is attached to the run as the "api-docs" artifact.
  docs:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v7
    - name: Set up JDK 21
      uses: actions/setup-java@v6
      with:
        java-version: '21'
        distribution: 'temurin'
    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v6
    - name: Generate API docs
      run: ./gradlew dokkaGenerate
    - uses: actions/upload-artifact@v7
      with:
        name: api-docs
        path: build/dokka/html
