# Module konstant-yaml

`YamlSource` reads YAML config through kaml. Mappings become dot-separated keys
(`server.port`) and sequences of plain values become lists.

# Package io.github.fiol_dev.konstant.yaml

The YAML source.
