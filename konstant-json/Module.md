# Module konstant-json

`JsonSource` reads JSON config through kotlinx-serialization-json. Nested objects become
dot-separated keys (`server.port`) and arrays of plain values become lists. Comments, trailing commas and a
leading BOM are accepted.

# Package io.github.fiol_dev.konstant.json

The JSON source.
