# ktoml 0.7.1's wasmJs klib produces an invalid module with Kotlin 2.3 (WebAssembly CompileError in
# TomlMainEncoder), so this module skips the wasmJs target until ktoml ships a fixed build.
konstant.wasmJs=false
