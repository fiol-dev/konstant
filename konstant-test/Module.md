# Module konstant-test

Helpers for tests. `MapSource` is an in-memory source built from key-value pairs, so tests
can load a config without files or environment variables.

# Package io.github.fiol_dev.konstant.test

Test sources.
