# Module konstant-koin

Koin integration: `config<T>()` inside a Koin module registers the config held by `Konstant` as a
single definition, so other definitions can inject it. Load the config before Koin resolves it.

# Package io.github.fiol_dev.konstant.koin

Koin module helpers.
